name: RCAN_New_Data
suffix: ~  # add suffix to saved images
model_type: SRModel
scale: 5
crop_border: ~  # crop border when evaluation. If None, crop the scale pixels
num_gpu: 1  # set num_gpu: 0 for cpu mode
manual_seed: 10

datasets:
  test_1:  # the 1st test dataset
    name: ERA52CERRA_V2
    type: NumpyPairedDataset
    dataroot_gt: /home/wj/ERA5toCERRA_NEW/processed_data/bundled/test_hr_norm.npy
    dataroot_lq: /home/wj/ERA5toCERRA_NEW/processed_data/bundled/test_lr_norm.npy
    norm_path: /home/wj/ERA5toCERRA_NEW/processed_data/bundled/manifest_train_scalers.npz
    io_backend:
      type: numpy

# network structures
network_g:
  type: RCAN
  num_in_ch: 1
  num_out_ch: 1
  num_feat: 64
  num_group: 10
  num_block: 20
  squeeze_factor: 16
  upscale: 5
  res_scale: 1
  img_range: 1.
  rgb_mean: [0.0]  # 무채널 데이터니까 걍 0으로

save_img: true

# path
path:
  pretrain_network_g: /home/wj/works/SR-project2/BasicSR/experiments/SCIE/RCAN_New_Data/models/net_g_18000.pth
  strict_load_g: true
  npy_results: ./results/npy_results  # 🔹 NumPy 저장 경로 추가
  gt_visualization: ./results/gt_visualization

# validation settings
val:
  save_img: true
  save_npy: true  # 🔹 NumPy 저장 여부 (False로 설정하면 저장 안 됨)
  suffix: ~  # add suffix to saved images, if None, use exp name

  metrics:
    psnr: {}  # ✅ 불필요한 'type' 제거
    ssim: {}
    rmse: {}
    mae: {}
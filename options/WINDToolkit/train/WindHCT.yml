# general settings
name: WindHCT_WINDToolkit
model_type: SRModel
scale: 5 # 5x upscaling
num_gpu: 1  # set num_gpu: 0 for cpu mode
manual_seed: 0

# dataset and data loader settings
datasets:
  train:
    name: WindToolkit
    type: NumpyPairedDataset
    dataroot_gt: datasets/WINDToolkit/train_hr_norm.npy # HR data path
    dataroot_lq: datasets/WINDToolkit/train_lr_norm.npy # LR data path
    filename_tmpl: '{}'
    io_backend:
      type: numpy
    gt_size: 100 # HR patch size
    use_hflip: false
    use_rot: false
    use_blur: false
    # data loader
    num_worker_per_gpu: 8
    batch_size_per_gpu: 32
    dataset_enlarge_ratio: 1 # no dataset enlargement
    prefetch_mode: ~

  val:
    name: WindToolkit
    type: NumpyPairedDataset
    dataroot_gt: datasets/WINDToolkit/val_hr_norm.npy
    dataroot_lq: datasets/WINDToolkit/val_lr_norm.npy
    norm_path: datasets/WINDToolkit/hr_stats.npz
    io_backend:
      type: numpy

network_g:
  type: WindHCT
  num_in_ch: 1
  num_out_ch: 1
  num_feat: 64
  window_size: 5
  num_heads: 4
  mlp_ratio: 4.0
  drop_prob: 0.1
  num_group: 6
  num_block: 6
  img_range: 1.0
  rgb_mean: [0.0]
  upscale: 5

# path
path:
  experiments_root: experiments/SCIE
  pretrain_network_g: ~
  strict_load_g: false
  resume_state: ~

train:
  ema_decay: 0.999
  optim_g:
    type: Adam
    lr: !!float 1e-4
    weight_decay: 0
    betas: [0.9, 0.99]

  scheduler:
    type: MultiStepLR
    milestones: [35000, 43000, 48000]  # based on total_iter = 50000
    gamma: 0.5

  total_iter: 50000
  warmup_iter: -1  # no warm up

  # losses
  pixel_opt:
    type: L1Loss
    loss_weight: 1.0
    reduction: mean

# validation settings
val:
  val_freq: !!float 1800  # validate every ~1 epoch
  save_img: true

  metrics:
    rmse: # metric name, can be arbitrary
      type: calculate_rmse
      crop_border: 0
      test_y_channel: false

# logging settings
logger:
  print_freq: 100
  save_checkpoint_freq: !!float 1800  # checkpoint every ~1 epoch
  use_tb_logger: true
  wandb:
    project: ~
    resume_id: ~

# dist training settings
dist_params:
  backend: nccl
  port: 29500
